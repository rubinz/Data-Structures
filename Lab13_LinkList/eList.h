// List.h#include "node.h"#ifndef ELIST#define ELISTclass ElephantList{  public:  		 ElephantList();	// CTOR	~ElephantList();	// DTOR		anElephant* create(void);	anElephant* create(string newName);	 bool IsEmpty(void) const;	 	 void Insert(anElephant*);	 	 void PrintAll(void) const;	  private:  	anElephant *head;		// pointer to a buffer node which in turns points to first node  	anElephant *tail;		// pointer to the last node on the list  	};#endif