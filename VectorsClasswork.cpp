#include <Vect.h>

Vect A(2), B(2), C(2);

// need dimensions like:





//Following items go in cpp:

//One = (1, 3)
One[0] = 1; //x component
One[1] = 3; //y component

Two[0] = 3;
Two[1] = 2;//Two = (3,2)
//add those two vectors to get vector C:
C = (4, 5)

