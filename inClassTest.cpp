#include <iostream>

using namespace std;

int main()
{
	//we could write std:: before cout if we wernt using namespace std
	cout << "hello world" << endl;
	return 0;
	//because main is an int we need to return a 0 at the end of it. 
	//use /n to do double space
}
